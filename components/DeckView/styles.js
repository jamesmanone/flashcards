import { StyleSheet } from 'react-native';
import baseStyle from '../../baseStyles';

export default StyleSheet.create({
  title: {
    fontSize: 36,
    fontWeight: 'bold',
    marginBottom: 10
  },
  noCards: {
    fontSize: 18,
    marginTop: 20
  }
});
